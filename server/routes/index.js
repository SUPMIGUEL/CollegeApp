var groups = require('./groups');
var users = require('./users');
var comments = require('./comments');
var homeworks = require('./homeworks');
var notes = require('./notes');
var projects = require('./projects');

module.exports = {
  users: users,
  groups: groups,
  comments: comments,
  homeworks: homeworks,
  notes: notes,
  projects: projects,
};